sudo: false
language: cpp
compiler: gcc
script: make test

matrix:  
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env:
        - CXX=g++-6 


